{{- if and .Values.collabora.ingress.enabled .Values.collabora.enabled }}
{{- $ingressClassName := .Values.collabora.ingress.ingressClassName | default .Values.global.ingress.ingressClassName }}
{{- $annotationsPreset := .Values.collabora.ingress.annotationsPreset | default .Values.global.ingress.annotationsPreset }}
{{- $annotations := merge (deepCopy .Values.collabora.ingress.annotations) (deepCopy .Values.global.ingress.annotations) }}
{{- $tlsEnabled := .Values.collabora.ingress.tls.enabled | default .Values.global.ingress.tls.enabled }}
{{- $tlsSecretName := .Values.collabora.ingress.tls.secretName | default .Values.global.ingress.tls.secretName }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "opencloud.fullname" . }}-collabora
  annotations:
    {{- if $annotationsPreset }}
    {{- include (printf "opencloud.ingress.annotations.%s" $annotationsPreset) . | nindent 4 }}
    {{- end }}
    {{- with $annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  {{- if $ingressClassName }}
  ingressClassName: {{ $ingressClassName | quote }}
  {{- end }}
  {{- if $tlsEnabled }}
  tls:
    - hosts:
        - {{ .Values.global.domain.collabora | quote }}
      {{- if $tlsSecretName }}
      secretName: {{ $tlsSecretName }}
      {{- end }}
  {{- end }}
  rules:
    - host: {{ .Values.global.domain.collabora | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "opencloud.fullname" . }}-collabora
                port:
                  number: 9980
{{- end }}
