{{- if .Values.opencloud.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "opencloud.opencloud.fullname" . }}
  labels:
    {{- include "opencloud.labels" . | nindent 4 }}
    app.kubernetes.io/component: opencloud
spec:
  type: ClusterIP
  ports:
    - port: 9200
      targetPort: http
      protocol: TCP
      name: http
    {{- if not .Values.opencloud.nats.external.enabled }}
    - port: 9233
      targetPort: 9233
      protocol: TCP
      name: nats
    {{- end }}
    {{- if or .Values.collaboration.collabora.enabled .Values.collaboration.external.enabled }}
    - port: 9300
      targetPort: collab-http
      protocol: TCP
      name: collab-http
    - port: 9301
      targetPort: collab-grpc
      protocol: TCP
      name: collab-grpc
    {{- end }}
  selector:
    {{- include "opencloud.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: opencloud
{{- end }}
