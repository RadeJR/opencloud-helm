{{- if and .Values.collaboration.onlyoffice.ingress.enabled .Values.collaboration.onlyoffice.enabled }}
{{- $ingressClassName := .Values.collaboration.onlyoffice.ingress.ingressClassName | default .Values.global.ingress.ingressClassName }}
{{- $annotationsPreset := .Values.collaboration.onlyoffice.ingress.annotationsPreset | default .Values.global.ingress.annotationsPreset }}
{{- $annotations := merge (deepCopy .Values.collaboration.onlyoffice.ingress.annotations) (deepCopy .Values.global.ingress.annotations) }}
{{- $tlsEnabled := .Values.collaboration.onlyoffice.ingress.tls.enabled }}
{{- $tlsSecretName := "" }}
{{- if .Values.collaboration.onlyoffice.ingress.tls.secretName }}
  {{- $tlsSecretName = .Values.collaboration.onlyoffice.ingress.tls.secretName }}
{{- else if .Values.global.ingress.tls.secretName }}
  {{- $tlsSecretName = .Values.global.ingress.tls.secretName }}
{{- else }}
  {{- $tlsSecretName = printf "%s-onlyoffice-tls" (include "opencloud.fullname" .) }}
{{- end }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "opencloud.fullname" . }}-onlyoffice
  annotations:
    {{- if $annotationsPreset }}
    {{- include (printf "opencloud.ingress.annotations.%s" $annotationsPreset) . | nindent 4 }}
    {{- end }}
    {{- with $annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  {{- if $ingressClassName }}
  ingressClassName: {{ $ingressClassName | quote }}
  {{- end }}
  {{- if $tlsEnabled }}
  tls:
    - hosts:
        - {{ .Values.global.domain.onlyoffice | quote }}
      secretName: {{ $tlsSecretName }}
  {{- end }}
  rules:
    - host: {{ .Values.global.domain.onlyoffice | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "opencloud.fullname" . }}-onlyoffice
                port:
                  number: 80
{{- end }}
